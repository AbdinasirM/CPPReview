#include <iostream>
using namespace std;

int main(){
    int age = 9;
    cout<<age<<endl; //outputs the value of the variable back
    cout<<&age<<endl; //outputs the address of where this variable is saved and also the adrress keeps changing randomly for security reasons


    return 0;
}